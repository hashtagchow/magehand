<?xml version="1.0" encoding="utf-8"?>
<!--
  MageHand launcher background (repainted 1.0.1). Deep navy sampled from the
  store-icon artwork so the raster foreground's feathered edge melts into it; the
  soft radial lift keeps the mark from looking pasted on at large sizes. No detail
  here is load-bearing — the background layer is parallaxed and cropped by the
  launcher's mask.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path android:pathData="M0,0h108v108h-108z">
        <aapt:attr xmlns:aapt="http://schemas.android.com/aapt" name="android:fillColor">
            <gradient
                android:type="radial"
                android:centerX="54"
                android:centerY="42"
                android:gradientRadius="72"
                android:startColor="#221B4A"
                android:endColor="#04072C" />
        </aapt:attr>
    </path>
</vector>
